

function PersonFunc(props) {

  return (
    
    <div>
      <span>{props.name}</span><br />
      <span>{props.surname}</span><br />
      <span>{props.age}</span>
    </div>
  );
}

export default PersonFunc;
